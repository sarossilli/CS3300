<% provide(:page_title,@project.title) %>

<div class="row">
  <div class="col-md-1 col-lg-2"></div>
  <div class="col-md-10 col-lg-8 text-xs-center">
    <h1><%= @project.title %></h1>
    
    <p><%= @project.description %></p>
    <h5>Technologies uesd: </h5>
    <p><%= @project.short_desc %></p>
    <%if current_user%>
    <%= link_to 'Edit', edit_project_url, class: "btn btn-lg"%>
    <%= link_to 'Destroy',  { action: :destroy, id: @project.id }, method: :delete, data: { confirm: 'Are you sure?' }, %>

    <%end%>
</div>